apiVersion: v1
kind: Service
metadata:
  name: diesel-gen
  labels:
    {{- include "ieee9-diesel.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ieee9-diesel.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
