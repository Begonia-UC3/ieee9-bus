## @section Image

## @param {string} imageTag="" - Override image tag. Defaults to the chart appVersion.
imageTag: ""

## @section Workload

## @param {int} replicas=1 - Replica count for diesel-gen.
replicas: 1

## @section Wiring

## @param {string} gridCentralUrl="http://grid-central:8000" - Base URL of the grid-central power-flow engine (in-namespace by default).
gridCentralUrl: "http://grid-central:8000"

## @param {string} tickInterval="1.0" - Seconds between telemetry ticks.
tickInterval: "1.0"

## @section Ingress

## @param {boolean} ingressEnabled=false - Expose diesel-gen via Ingress.
ingressEnabled: false

## @param {string} ingressHost="diesel.example.com" - Ingress host when enabled.
ingressHost: "diesel.example.com"

## @param {string} ingressClassName="" - Optional IngressClass name.
ingressClassName: ""

## @param {boolean} tlsEnabled=true - Issue an LE certificate via cert-manager when ingress is enabled.
tlsEnabled: true

## @param {string} tlsClusterIssuer="letsencrypt-prod" - cert-manager ClusterIssuer used for the certificate.
tlsClusterIssuer: "letsencrypt-prod"
